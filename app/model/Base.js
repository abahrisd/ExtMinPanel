/**
 * This model is the base for all other models in this application.
 */
Ext.define('ExtMinPanel.model.Base', {
    extend: 'Ext.data.Model',

    schema: {
        namespace: 'ExtMinPanel.model'
    }
});
